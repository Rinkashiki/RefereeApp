<div class="container form_padding">
    <br>
    <div class="form-group">
      <%= form_for @activity, remote: true do |a| %>
        <% flash.each do |type, msg| %>
          <div class="alert alert-danger alert-dismissible d-inline-block" role="alert">
            <%= msg %>
          </div> 
        <% end %>
        <% flash.delete(:alert) %>
        <p><%= link_to 'Ocultar formulario', activities_path, remote: true, class: 'btn btn-primary' %></p>
        <h1>Nueva Actividad</h1>
        <p>
          <%= a.label :Nombre %><br>
          <%= a.text_field :name, class: "textBox" %>
        </p>
        <p>
          <%= a.submit 'Crear', class: 'btn btn-primary' %>
        </p>
      <% end %>
    </div>
  </div>
